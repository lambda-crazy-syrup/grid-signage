{
  "name": "grid-signage",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run --filter=signage-host dev",
    "build": "bun run --filter=signage-host build",
    "type-check": "bun run --filter=signage-host type-check"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0"
  }
}
